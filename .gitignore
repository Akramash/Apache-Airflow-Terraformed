# Ignore Terraform state files
terraform.tfstate
terraform.tfstate.backup

# Ignore crash logs
crash.log

# Ignore Terraform lock file
.terraform.lock.hcl

# Ignore the .terraform directory (which contains local modules and plugins)
.terraform/

# Ignore personal and sensitive files (SSH keys, credentials)
*.pem
*.key
